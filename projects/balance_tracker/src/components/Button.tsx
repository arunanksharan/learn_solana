import React from "react";

const Button = () => {
  return (
    <button
      type="submit"
      className="bg-button w-[320px] h-[48px] text-white text-2xl rounded-[62px] font-mono transform hover:scale-105"
    >
      Check Sol Balance
    </button>
  );
};

export default Button;
